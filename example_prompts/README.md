# Примеры промптов

Образцы текстов промптов A и B для A/B-теста постов LR в Telegram.

Рабочие промпты задаются в `config.py` и при необходимости в папке `prompts/` (она не выгружается в репозиторий).
