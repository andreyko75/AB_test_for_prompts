# A/B-тестирование промптов: посты LR для Telegram

Проект для сравнения двух вариантов промпта (A и B) при генерации продающих постов для Telegram по описаниям товаров LR. Включает датасет, ручную оценку метрик, формирование отчёта и опциональное сравнение с оценкой AI (LLM-as-a-Judge).

![Python](https://img.shields.io/badge/Python-3776AB?logo=python&logoColor=white)
![OpenAI](https://img.shields.io/badge/OpenAI-412991?logo=openai&logoColor=white)
![python-dotenv](https://img.shields.io/badge/python--dotenv-3776AB?logo=python&logoColor=white)

---

## 📋 Содержание

* Описание
* Функционал
* Установка и настройка
* Использование
* Структура проекта
* Что выгружается в GitHub
* Лицензия

---

## 🎯 Описание

В рамках задания по курсу A/B-тестирования реализован пайплайн:

* **Датасет** — описания товаров в `dataset/product_descriptions.json` (папка `dataset/` не выгружается в репозиторий).
* **Два промпта** — контрольный (A) и с акцентом на медицинскую терминологию (B); тексты в `config.py`. Папка `prompts/` для своих копий — тоже не выгружается.
* **Генерация ответов** — скрипт запрашивает у OpenAI (gpt-4o-mini) ответы для каждого описания по вариантам A и B, сохраняет в `output/results.json`.
* **Ручная оценка** — в `results.json` проставляются релевантность (1–5) и галлюцинации (0/1).
* **Отчёт** — скрипт считает метрики и формирует отчёт; при наличии `OPENAI_API_KEY` добавляется сравнение с оценкой AI.

Primary-метрика: релевантность. Guard-метрика: галлюцинации. MDE = 0.3.

---

## 🚀 Функционал

* Генерация постов для двух вариантов промпта на одном датасете.
* Сохранение результатов в JSON с полями для ручной оценки и латентности.
* Расчёт средних по релевантности и доле галлюцинаций, проверка MDE и guard.
* Опционально: оценка тех же ответов моделью (LLM-as-a-Judge) и сравнение с вашими оценками в отчёте.
* Демо-режим без API: создание примерного `results.json` для проверки шага оценки.

---

## ⚙️ Установка и настройка

### Требования

* Python 3.10+
* API-ключ OpenAI (для полного прогона)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/andreyko75/AB_test_for_prompts.git
cd AB_test_for_prompts
```

### Шаг 2: Виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate   # macOS/Linux
# venv\Scripts\activate   # Windows
```

### Шаг 3: Зависимости

```bash
pip install -r requirements.txt
```

### Шаг 4: Переменные окружения

Скопируйте `.env.example` в `.env` и укажите ключ:

```bash
cp .env.example .env
# В .env: OPENAI_API_KEY=sk-...
```

**Важно:** файл `.env` в `.gitignore` и не попадёт в репозиторий.

### Шаг 5: Датасет (при клонировании чужого репо)

Папка `dataset/` не выгружается в репозиторий. Если вы клонировали проект, создайте `dataset/product_descriptions.json` сами (список объектов с полями `id`, `description`, при необходимости `note`). Формат можно взять из кода в `run_ab_test.py`.

---

## 💻 Использование

### Шаг 1 — Генерация ответов A/B

```bash
python run_ab_test.py
```

Создаётся `output/results.json`: для каждого описания — ответы модели по промпту A и B, поля `relevance_a`, `relevance_b`, `hallucination_a`, `hallucination_b` (изначально `null`).

### Шаг 2 — Ручная оценка

Откройте `output/results.json`. В каждом элементе массива `items` замените `null` на числа:

| Поле | Значения | Описание |
|------|----------|----------|
| `relevance_a` / `relevance_b` | 1–5 | Релевантность поста (соответствие описанию, продающая сила). |
| `hallucination_a` / `hallucination_b` | 0 или 1 | Есть ли выдуманные факты о здоровье (0 — нет, 1 — да). |

Сохраните файл.

### Шаг 3 — Отчёт

```bash
python evaluate_and_report.py
```

Появятся `output/report.txt` и `output/dataset.txt`. При заданном `OPENAI_API_KEY` в отчёт добавится блок сравнения ваших оценок с оценкой AI.

### Демо без API

Чтобы только проверить формирование отчёта без вызова OpenAI:

```bash
python create_demo_results.py
# Проставьте метрики в output/results.json (хотя бы в нескольких элементах)
python evaluate_and_report.py
```

---

## 📝 Структура проекта

```
ped04/
├── .env.example
├── .gitignore
├── README.md
├── config.py
├── requirements.txt
├── run_ab_test.py
├── evaluate_and_report.py
├── create_demo_results.py
├── example_dataset/           # Пример датасета (в репо)
│   ├── README.md
│   └── product_descriptions_example.json
├── example_prompts/           # Примеры промптов (в репо)
│   ├── README.md
│   ├── prompt_a_example.md
│   └── prompt_b_example.md
├── example_output/            # Пример отчёта и вывода (в репо)
│   ├── README.md
│   ├── report_sample.txt
│   └── dataset_sample.txt
├── dataset/                   # Рабочий датасет (локально, не в репо)
│   └── product_descriptions.json
├── prompts/                   # Ваши промпты (локально, не в репо)
│   └── ...
└── output/                    # Результаты запусков (не в репо)
    └── ...
```

---

## 📤 Что выгружается в GitHub

В репозиторий **попадают**:

* Исходный код и конфиг: `config.py`, скрипты, `requirements.txt`, `.env.example`, `.gitignore`.
* **Примеры для показа** (каталоги с префиксом `example_`):
  * `example_dataset/` — образец датасета (`product_descriptions_example.json`);
  * `example_prompts/` — образцы промптов A и B (`prompt_a_example.md`, `prompt_b_example.md`);
  * `example_output/` — пример отчёта и фрагмент датасета в текстовом виде.

В репозиторий **не попадают** (указаны в `.gitignore`):

* `.env` — секреты.
* `output/` — результаты запусков.
* **`dataset/`** — ваш датасет (с чем работаете) остаётся только у вас.
* **`prompts/`** — ваши файлы с промптами остаются только у вас.
* `venv/`, `.venv/`, `__pycache__/` и т.п.

Тексты промптов A и B прописаны в `config.py` (файл в репо). Если не хотите светить их в открытом доступе, вынесите в отдельный файл и подгружайте его в коде; сам файл положите в `prompts/` (папка не выгружается).

---

## 📄 Лицензия

Проект создан в учебных целях (курс по A/B-тестированию). Используйте в соответствии с условиями OpenAI API и правилами учебного заведения.

---

_Домашнее задание: A/B-тестирование промптов для постов LR в Telegram_
