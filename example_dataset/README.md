# Пример датасета

Файл `product_descriptions_example.json` — образец структуры датасета для A/B-теста.

Каждый элемент: `id`, `description`, при необходимости `note`.  
Рабочий датасет хранится в папке `dataset/` (не выгружается в репозиторий).
